---
import type { HTMLTag, Polymorphic } from "astro/types";
import "../styles/global.css";
import Icon from "./Icon.astro";

type Props<Tag extends HTMLTag> = Polymorphic<{
  as: Tag;
  icon?: "arrowRight" | "chevronRight" | "download" | "linkedin";
}>;

const { as: Tag = "button", icon = "download", ...props } = Astro.props;
---

<Tag {...props}><slot /><Icon name={icon} class="icon" /></Tag>

<style lang="scss">
  button,
  a {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: var(--space-xs) var(--space-m);
    border: var(--border-size-regular) solid var(--color-border);
    background-color: var(--color-background);
    border-radius: var(--border-radius-regular);
    cursor: pointer;
    text-align: left;
    transition: border-color var(--transition-duration-regular);

    &:hover {
      border-color: var(--color-border-hover);

      .icon {
        color: var(--color-content-regular);
      }
    }

    .icon {
      height: var(--step-1);
      width: var(--step-1);
      color: var(--color-content-subtle);
      transition: color var(--transition-duration-regular);
    }
  }

  a {
    text-decoration: none;
  }
</style>
