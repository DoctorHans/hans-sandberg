---

---

<ul>
  <slot />
</ul>

<style lang="scss">
  ul {
    list-style-type: "";
    padding: 0;
    display: flex;
    flex-direction: row;
    gap: var(--space-m);
    overflow-x: scroll;
    scroll-snap-type: x mandatory;
    margin-block-start: var(--space-l);
  }
</style>
